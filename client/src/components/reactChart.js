import React from 'react';
import {connect} from 'react-redux';
// combine form with chart in a new module
export default function reactChart(){
	return (
		<div>
			<h1>React Chart</h1>
		</div>
	)
};